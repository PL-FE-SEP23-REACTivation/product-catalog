export type Quantity = {
  quantity: number;
};

export type AllQuantity = {
  accessories: number | undefined;
  phones: number | undefined;
  tablets: number | undefined;
};

export type CategoryCount = {
  category: string;
  count: number;
};
